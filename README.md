# Multi-Platform Matrix Build with Artifacts

This repository demonstrates a GitHub Actions multi-platform matrix build
that produces and uploads artifacts per matrix variant.

- Matrix: `ubuntu-latest`, `macos-latest`, `windows-latest`
- Artifact names: `build-13126ee-<os>`
- Identifier step: `matrix-13126ee`

**Contact:** your.name@example.com
